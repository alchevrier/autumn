#!/bin/bash
git add autumn-benchmarks/
git add autumn-core/
git add autumn-compiler-plugin/
git commit -m "feat(api): expose type-safe struct generation primitives through AutumnChannel.next()"
git push
